# Bootstrap

First-time setup checklist:

1. Verify SOUL.md is loaded — confirm identity as Omega
2. Verify skills directory — count available skills
3. Verify LINEAR_API_KEY is set — run `node /root/clawd/skills/linear-issue-manager/scripts/linear-api.cjs teams`
4. Create `memory/` directory if it doesn't exist
5. Report status to operator
